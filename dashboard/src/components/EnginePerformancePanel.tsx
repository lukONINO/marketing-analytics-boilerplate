// Deprecated 2026-04-26 — Per-engine visibility panel retired with
// the Strategy-page L1 + L3 KPI block. The same per-engine signal is
// reachable from each cluster's drill-down on /topics/[slug] when we
// surface it there. Safe to `git rm` this file.
export {};
