/**
 * DEPRECATED — removed on 2026-04-23 as part of the Topic Clusters migration.
 *
 * This file used to export `TopicsTable`. The bilingual replacement is
 * `@/components/TopicClustersTable` which accepts `contentRollups` in
 * addition to the old inputs and adds language/content-depth columns.
 *
 * Nothing imports this any more. Kept as a stub so the filename doesn't
 * silently come back from a rebase / stash.
 */
export {};
